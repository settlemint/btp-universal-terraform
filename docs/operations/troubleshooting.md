# Troubleshooting

Common issues and fixes across local and cloud profiles.

- Terraform provider auth errors: verify credentials and selected subscription/project.
- Helm release failed: check `kubectl describe` events and pod logs.
- Ingress unreachable: validate DNS/hosts, TLS certs, and ingress class.
- Secrets access: confirm backend policies/roles and secret paths.

